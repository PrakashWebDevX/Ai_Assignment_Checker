// Firebase configuration disabled - using mock authentication instead
// If you need Firebase in the future, restore this file from git history

export const app = null;
export const auth = null;
export const googleProvider = null;
